name: Sync to HuggingFace

on:
  push:
    branches: [main]

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Filter large binaries and push to HuggingFace
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          # Rewrite history to strip binaries that exceed HF's 10MB limit
          pip install git-filter-repo
          git filter-repo --path-glob 'tui/packages/*/bin/lingtai' --invert-paths --force

          # HF requires token-based auth via credential helper
          git config credential.helper '!f() { echo "username=huangzs"; echo "password=${HF_TOKEN}"; }; f'
          git remote add hf https://huggingface.co/huangzs/lingtai
          git push hf main --force
