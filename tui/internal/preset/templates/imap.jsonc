// =============================================================
// IMAP Email — imap.json
// =============================================================
// Point to this file from init.json:
//   "addons": { "imap": { "config": "path/to/this/config.json" } }
//
// Passwords go in your .env file (see env_file in init.json),
// NOT in this config file. Use email_password_env to reference
// the environment variable name.
//
// For Gmail, use an App Password (not your regular password):
//   https://myaccount.google.com/apppasswords
//
// This example ships LOCKED DOWN: only the senders listed in
// "allowed_senders" below are processed. Inbound mail can carry
// instructions to an agent with real side effects (prompt-injection
// surface), so keep this allowlist populated.
// =============================================================
{
  // Only accept emails from these senders.
  // Replace with the addresses that may reach this agent.
  "allowed_senders": ["boss@example.com", "team@example.com"],

  // Email address to poll.
  "email_address": "agent@example.com",

  // Environment variable holding the email password / app password.
  "email_password_env": "IMAP_PASSWORD",

  // IMAP server settings.
  "imap_host": "imap.gmail.com",
  "imap_port": 993,

  // SMTP server settings (for sending replies).
  "smtp_host": "smtp.gmail.com",
  "smtp_port": 587

  // Optional: seconds between inbox checks (default 60).
  // "poll_interval": 60

  // UNSAFE variant — open access: all incoming mail is processed.
  // Only for throwaway agents without bash/mail/file capabilities.
  // To use: delete "allowed_senders" above.
}
